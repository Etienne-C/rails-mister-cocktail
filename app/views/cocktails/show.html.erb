<div style='background-image: url("<%= @cocktail.photo.url(:landscape) %>"); background-size: cover; height: 40vh;'>

</div>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <h1><%= @cocktail.name %></h1><br></br>
      <br>
      <%= link_to 'Delete', cocktail_path(@cocktail), method: 'delete' %>
      |
      <%= link_to 'Back', cocktails_path %>
      |
      <%= link_to 'New Dose', new_cocktail_dose_path(@cocktail) %>
    </div>
  </div>
  <br>
  <br>
  <br>
  <div class="row">
    <%# @doses.each do |dose| %>
      <%#= render partial: 'doses/dose', locals: { dose: dose} %>
    <%# end %>

    <table class='table'>
      <%= render @doses %>
    </table>
  </div>
</div>
